<!-- e0756b55-59d5-46d8-97d3-953a384c05b5 2aba5dd6-bcc0-479c-81f3-b8563dfc39a5 -->
# Spike Clip Visualization Script

- Create `spkvisual` folder to hold the visualization script and any helper modules.
- Read `options/gopro_rgbspike_local.json` to get default spike clip root/config (spike_h/w, flip flags, ext, folder names); allow CLI override of clip path/output dir.
- Use SpikeCV loader (`SpikeCV/spkData/load_dat.py` -> `SpikeStream.get_spike_matrix`) to load the selected `.dat` clip with correct height/width and flip settings.
- Iterate frames and save as image sequence (e.g., PNG) at original size to an output folder; include minimal CLI help and notes in code comments.
- Add a short usage note in README or script docstring on how to point to another clip path.

### To-dos

- [ ] Parse options json for default spike paths/config
- [ ] Load .dat clip via SpikeStream with shape/flip cfg
- [ ] Write frames to image sequence preserving size
- [ ] Document script usage and defaults